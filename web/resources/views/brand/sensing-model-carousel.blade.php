{{-- <div class="owl-carousel owl-theme model-selection-container half-bg">

    @foreach (config('global.allmodels') as $item)
        @if (in_array($item->slug, ['type-r','brv','city','hrv','jazz']))

            @include('components.model-carousel-item')

        @endif
    @endforeach

</div> --}}
